import tape from 'tape'
import _test from 'tape-promise'
import sinon from 'sinon'
import Rx from 'rxjs/Rx'

const test = _test(tape)

test('Rx Sender Tests', (t) => {

  t.test('Rx Sender Observable Tests', (t) => {
    t.end()
  })

  t.test('Rx Sender Observer Tests', (t) => {
    t.end()
  })

  t.test('Rx Sender Disposable Tests', (t) => {
    t.end()
  })

  t.test('Rx Sender Subject Tests', (t) => {
    t.end()
  })  
})